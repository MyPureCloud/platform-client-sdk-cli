package models
import (
    "encoding/json"
    "strconv"
    "strings"
)

var (
    WheelpickerMarshalled = false
)

// This struct is here to use the useless readonly properties so that their required imports don't throw an unused error (time, etc.)
type WheelpickerDud struct { 
    


    

}

// Wheelpicker - Wheel picker component with selectable items
type Wheelpicker struct { 
    // Id - Unique identifier for the wheel picker (generated by backend)
    Id string `json:"id"`


    // Items - Items in the wheel picker
    Items []Wheelpickeritem `json:"items"`

}

// String returns a JSON representation of the model
func (o *Wheelpicker) String() string {
    
     o.Items = []Wheelpickeritem{{}} 

    j, _ := json.Marshal(o)
    str, _ := strconv.Unquote(strings.Replace(strconv.Quote(string(j)), `\\u`, `\u`, -1))

    return str
}

func (u *Wheelpicker) MarshalJSON() ([]byte, error) {
    type Alias Wheelpicker

    if WheelpickerMarshalled {
        return []byte("{}"), nil
    }
    WheelpickerMarshalled = true

    return json.Marshal(&struct {
        
        Id string `json:"id"`
        
        Items []Wheelpickeritem `json:"items"`
        *Alias
    }{

        


        
        Items: []Wheelpickeritem{{}},
        

        Alias: (*Alias)(u),
    })
}

