package models
import (
    "encoding/json"
    "strconv"
    "strings"
)

var (
    PushdeviceupdaterequestMarshalled = false
)

// This struct is here to use the useless readonly properties so that their required imports don't throw an unused error (time, etc.)
type PushdeviceupdaterequestDud struct { 
    


    

}

// Pushdeviceupdaterequest
type Pushdeviceupdaterequest struct { 
    // Language - The preferred language of the user (eg. en-us, de, fr)
    Language string `json:"language"`


    // DeviceToken - The device token generated by the network providers
    DeviceToken string `json:"deviceToken"`

}

// String returns a JSON representation of the model
func (o *Pushdeviceupdaterequest) String() string {
    
    

    j, _ := json.Marshal(o)
    str, _ := strconv.Unquote(strings.Replace(strconv.Quote(string(j)), `\\u`, `\u`, -1))

    return str
}

func (u *Pushdeviceupdaterequest) MarshalJSON() ([]byte, error) {
    type Alias Pushdeviceupdaterequest

    if PushdeviceupdaterequestMarshalled {
        return []byte("{}"), nil
    }
    PushdeviceupdaterequestMarshalled = true

    return json.Marshal(&struct {
        
        Language string `json:"language"`
        
        DeviceToken string `json:"deviceToken"`
        *Alias
    }{

        


        

        Alias: (*Alias)(u),
    })
}

